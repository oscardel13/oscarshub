version: '3'

services:
  api:
    build:
      context: ./server
      dockerfile: dockerfile.dev  

  client:
    build: 
      context: ./client
      dockerfile: dockerfile

  nginx:
    restart: always
    depends_on:
      - api
      - client
    build: 
      context: ./nginx
      dockerfile: dockerfile.dev
    ports:
      - 8000:80